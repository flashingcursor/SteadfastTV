<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- libmpv declares minSdk 26 for AAudio, but mpv falls back to OpenSL ES at runtime (both are
         compiled in) and the app falls back to ExoPlayer if mpv fails entirely — verified on a
         Fire OS 6 (API 25) Toshiba Fire TV Edition. -->
    <uses-sdk tools:overrideLibrary="dev.jdtech.mpv" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- In-app updates: hand the downloaded release APK to the system installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Downloads keep transferring after the user leaves the app, as a foreground service with an
         ongoing progress notification. POST_NOTIFICATIONS is only asked for by the system on API 33+;
         if it is denied the service still runs, the notification is just not shown. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Volume boost above 100% on Live TV: LoudnessEnhancer attaches an audio effect to the player's
         own audio session. Normal permission, granted at install; no runtime prompt. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Android TV provider writes for Watch Next cards. -->
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_DATA" />

    <!-- Downloads & backup to user-chosen folders (SAF's document picker is absent on most TVs).
         READ stays declared through API 32: dropping it left some OEM builds (TCL Android 12,
         "Permission Shield") with NO grantable storage entry at all — MANAGE_EXTERNAL_STORAGE alone
         is a special app access their settings never surface. With READ declared, requesting it on
         API 30–32 opens the system "Files and media" screen, which also lists
         "Allow management of all files" because MANAGE is declared. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- TV-first app, but allow non-TV installs too: touchscreen optional, and leanback is NOT required
         (required=true blocked plain phones / non-TV armv7a boxes that don't report the leanback feature). -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- android:icon points at tv_tile, a square tile designed for Fire TV: its launcher
         ignores android:banner for sideloaded apps and constrains android:icon to a square
         inner box on the home tile, drawn over the launcher's own grey tile (verified
         empirically; full-bleed 16:9 tiles only come with Amazon Appstore submission). The
         tile is the stacked lockup on a TRANSPARENT background so it blends into that grey
         instead of floating as a colored square. Android TV/Google TV launchers use
         android:banner (tv_banner, 16:9) and never read this. -->
    <application
        android:name=".OwnTVApp"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tv_tile"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OwnTV"
        android:usesCleartextTraffic="true"
        tools:targetApi="26">

        <!-- Exposes the downloaded update APK to the system installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Disable WorkManager default auto-init; OwnTVApp provides Configuration. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- WorkManager's foreground-service host, typed for the download worker's data sync. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <activity
            android:name=".MainActivity"
            android:banner="@drawable/tv_banner"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.OwnTV.Starting">
            <!-- LEANBACK_LAUNCHER shows it on TV launchers; LAUNCHER shows it on phone/tablet/box home
                 screens (so non-TV devices get an app icon, not just an install). -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="play"
                    android:scheme="owntv" />
                <data
                    android:host="open"
                    android:scheme="owntv" />
            </intent-filter>
        </activity>

    </application>

</manifest>
